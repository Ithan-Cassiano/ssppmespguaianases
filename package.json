{
  "name": "portal-pf-virtual",
  "version": "1.0.0",
  "private": true,
  "description": "Portal institucional da Polícia Federal Virtual com painel de notícias.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --env-file=.env dev-server.js",
    "seed": "node --env-file=.env scripts/seed.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4"
  }
}
